import { Inter, Corben } from 'next/font/google';

export const baseFont = Inter({
  subsets: ['latin'],
  weight: ['400']
});

export const corben = Corben({
  subsets: ['latin'],
  weight: ['400']
});
