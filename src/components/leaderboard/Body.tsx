'use client';
import styled from 'styled-components';

const Container = styled.section`
  display: flex;
  align-items: center;
  flex-wrap: wrap;

  gap: ${16 / 16}rem;
`;

const Body = () => {
  return <Container>empty</Container>;
};

export default Body;
