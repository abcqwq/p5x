'use client';
import styled from 'styled-components';
import { corben } from '@/fonts';

const Container = styled.h1``;

const Header = () => {
  return (
    <Container className={corben.className}>abcqwq starter things</Container>
  );
};

export default Header;
